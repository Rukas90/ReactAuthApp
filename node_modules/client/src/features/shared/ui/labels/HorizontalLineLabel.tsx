const HorizontalLineLabel = ({
  children,
  className,
}: Pick<React.ComponentProps<"div">, "children" | "className">) => {
  const line = <div className={`grow h-px bg-stone-800`} />
  return (
    <div
      className={`${className} flex flex-row justify-center items-center gap-4`}
    >
      {line}
      <p className="p-0 m-0">{children}</p>
      {line}
    </div>
  )
}
export default HorizontalLineLabel
